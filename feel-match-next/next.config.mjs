/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Si tu veux activer React Strict Mode
    swcMinify: true, // Active la minification SWC
  };
  
  export default nextConfig;
  